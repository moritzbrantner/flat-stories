# AGENTS.md

Flat Stories is a product-first SVG editor. Keep browser interaction, React state, and SVG rendering in TypeScript. Computational implementations may sit behind `EditorEngine`; do not introduce Rust/WASM until a concrete workload in A1 needs it.

Run `bun run check` before completion. Keep focused tests beside editor code.
